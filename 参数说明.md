# LLama 启动器 · 参数说明

> **当前版本：v0.3.85**　·　本文档对界面中每一个设置项做详细说明。界面中鼠标悬停在任意参数上也会显示简版提示。

---

## 〇、本版（v0.3.85）更新要点

- **窗口默认尺寸** 调整为 **1280 × 1280**。
- **生成参数区重新排布**：三组「标签 + 控件」均匀铺满整行，标签不再被挤压隐藏。
- **高级参数区压缩**：连续批处理 / mlock / mmap / MTP + MTP 说明排到**同一行**；CPU线程、API Key 不再独占宽度，各控件**平均分布**。
- **底部十个功能按钮**压缩宽度（150 → 120），右侧广告位**横向填充**剩余空间。
- **模型下载页全新改版**：左右双列（左=仓库、右=仓库内文件），A/B/C 三栏检索（组织 / 模型名称 / 辅助关键词 + 并/或），每页数量可选，「加载更多」「打开下载目录」，文件行含 文件名/大小/已安装/下载/进度，点击已安装文件即切换主模型；下方新增「仓库信息」「操作日志」，并带可上下拖动的比例条。
- **模型下载目录**默认 = 主页「模型目录」，并按仓库名建子文件夹。
- **MTP 逻辑优化**：识别 `mtp / nextn / eh_proj / shared_head` 及元数据 `nextn_predict_layers`，支持的模型可正常开启；检测不到时也不再强制禁用，可手动勾选。
- **主题配色独立成文件**（`src/themes.py`）：深色 / 浅色 / 橙色 / 绿色 / 灰色 / 棕色，**切换主题不改变布局尺寸**；浅色边框加深，分割线更清晰。
- **模型路径** 显示在**底部状态栏左侧**。
- **模型下载页的仓库/文件表**支持点击表头排序；移除日期列。
- **导出启动文件**不再在导出目录产生多余的 `.ico` 图标文件（快捷方式改用启动器内嵌图标）。
- 版本号更新为 **0.3.85**；配置文件补齐 `custom_tools` / `opencode_workdir` / `last_download_dir` 等键。

---

## 一、基本参数（默认展开）

### 1. llama 目录
- **是什么**：llama.cpp 的安装目录。
- **要求**：目录下应有 `llama-server.exe` 及配套的 DLL（`llama.dll`、`ggml-*.dll` 等）。这是从官方 Release 解压后得到的完整文件夹。
- **作用**：程序从这里读取 `llama-server.exe` 来启动服务，并在此目录（含所有子文件夹）内递归扫描模型。

### 2. 主模型
- **是什么**：要加载的 GGUF 格式模型文件（`*.gguf`）。
- **怎么选**：程序自动扫描 llama 目录下所有 `.gguf` 文件（自动跳过未下载完成的 `.xltd` / `.tmp` 等文件），下拉列表中显示文件名与大小。你只需要点选。
- **排序（在主模型右侧）**：紧挨「主模型」右侧的「排序」下拉可切换排序方式（详见「七、1」）。
- **模型完整路径**：当前所选模型的绝对路径显示在**底部状态栏最左侧**（鼠标悬停可看完整路径）。
- **视觉模型提示**：若该模型是「多模态/视觉模型」，通常需要配合一个 `mmproj` 文件一起加载。

### 3. 视觉模型（mmproj）
- **是什么**：多模态视觉模型文件（`mmproj-*.gguf`），负责把图像编码成模型能理解的向量。没有它，模型只能处理纯文本。
- **自动配对规则**：**严格按目录配对** —— 只自动选中「与主模型在同一个目录下」的 mmproj，从而保证主模型与视觉模型是配套一致的。
  - 例如 `models/` 目录下同时有 `Qwythos-...BF16.gguf` 和 `mmproj-Qwythos-...F16.gguf`，选择前者时会自动选中后者。
  - 若主模型所在目录没有 mmproj（例如纯文本/代码模型），则默认「（不使用）」，不会错误地配到别的目录。
- **维度一致性校验**：启动前会自动读取主模型与视觉模型的嵌入维度（projection_dim / embedding_length）并比对。不一致时会**红色警告并弹出确认框**（如：主模型 2560 vs 视觉模型 5376），此时应更换配套的视觉模型，或选择「（不使用）」以纯文本模式运行。
- **提示**：下拉列表只显示文件名（悬停可看完整路径）；若有同名文件才附带所在文件夹名。
- 你也可以**手动**在下拉列表里改选其他目录的 mmproj，或选择「（不使用）」以纯文本模式运行。

### 4. 预设
- **是什么**：根据显卡显存大小，一键设置「推荐的上下文长度」。
- **只改一个参数**：选择预设后**只修改「上下文」**这一项，其余参数一律不动。
- **对照表**：

| 预设 | 上下文长度 | 说明 |
|------|-----------|------|
| 4GB 显存 | 16K（16384） | 小显存也能跑 |
| 6GB 显存 | 32K（32768） | 默认推荐档 |
| 8GB 显存 | 64K（65536） | |
| 12GB 显存 | 128K（131072） | |
| 16GB 显存 | 256K（262144） | |
| 24GB 显存 | 512K（524288） | 长上下文、大显存 |

- **提示**：预设给的是「显存够用」的保守值；显存越少能开的上下文越短。想精确控制就选「自定义」，直接改「上下文」数值。

### 5. GPU 层数（-ngl）——位于「生成参数」分组
- **是什么**：把模型计算层卸载到显卡显存的层数。（已从基本参数区移入「生成参数」分组，与重复惩罚同一行。）
- **常用取值**：
  - `all`：尽可能全部放进显存，速度最快（显存放得下时首选）。
  - `auto`：由程序自动判断。
  - `0`：完全用 CPU 跑（不占显存，但慢很多）。
  - 具体数字（如 `10`、`40`）：手动指定只放前 N 层。
- **经验**：显存够大就 `all`；显存不够会加载失败，此时改为 `0` 或较小的数值，或用 CPU 模式。
- **与多显卡的关系（不冲突）**：`-ngl` 决定**总共**卸载多少层到显存；勾选 ≥2 张卡后，再由「多显卡」区的 `-ts`(tensor-split) / `-sm`(拆分模式) 决定这些层如何在多张卡之间分配。两者是互补关系，可同时使用。

### 6. 监听地址（Host）
- **是什么**：服务监听的网络地址。
- `127.0.0.1`：仅本机可访问（最安全，默认）。
- `0.0.0.0`：局域网内其他设备也能访问（注意安全，建议配合 API Key）。

### 7. 端口
- **是什么**：服务监听的端口，默认 `8080`。
- 启动成功后，OpenAI 兼容接口地址为 `http://监听地址:端口/v1`，Web 界面为 `http://监听地址:端口/`。
- 如果端口被占用，启动前程序会提示。

### 8. 上下文（-c，ctx size）
- **是什么**：上下文长度（单位：token），即模型**一次会话最多能「记住」多少内容**（你的历史对话 + 它的回答）。
- **数值含义**：值越大，模型能参考的对话越长，但占用的显存/内存越大、速度越慢。
- **上限参考**：「模型信息」页会显示该模型的「训练上下文」，例如 `262144`，意思是这个模型原生最多支持 262K token 的上下文。启动器里设的上下文应尽量**不超过**这个值（超过会因 KV 缓存不足而报错或异常）。
- **最小建议**：一般至少 4096，做代码/长文推荐 8192 以上；要跑 1M 上下文的模型（如你的 Qwythos-1M）则按预设选 512K。

### 9. 预测 Token（-n，n_predict）
- **是什么**：模型**每次回答最多生成的 token 数**。
- `-1` = 无限：一直生成直到模型输出结束符为止。
- 具体数字（如 8192）：限制单次回答最长 8192 token。
- **用途**：当模型「停不下来 / 无限复读 / 输出失控」时，调小这个值可以强行截断回答长度，是缓解幻觉复读的实用手段之一。

---

## 二、高级参数（默认折叠，按需打开）

### 1. CPU 线程（-t）
- **是什么**：用于计算（推理 + 部分 prompt 处理）的 CPU 线程数。
- `-1` = 自动（通常使用全部逻辑核心）。一般无需修改。

### 2. 批大小（-b，batch size）
- **是什么**：逻辑批大小，影响 prompt 处理速度。
- 默认 2048 通常就很好；除非排查性能问题，一般不用改。

### 3. 并行 Slots（-np，parallel）
- **是什么**：服务能**同时处理**多少个并发请求。
- `-1` = 自动。多客户端（opencode、Cherry Studio、浏览器同时用）建议设为 2~4。
- **代价**：每个 slot 会额外占用一份上下文内存，slot 越多越占显存。

### 4. 闪存注意力（-fa，Flash Attention）
- **是什么**：是否启用 Flash Attention 优化。
- `on`：推荐。显著省显存、提升长上下文速度（现代显卡均支持）。
- `off`：个别老显卡或兼容问题才需要关。
- `auto`：由程序判断。

### 5. KV 缓存 K / V 独立类型（-ctk / -ctv）
- **是什么**：KV 缓存的存储精度，直接决定「上下文能占多大显存」。**K（Key）与 V（Value）可分别独立设置**：
  - 「KV缓存K(-ctk)」= `-ctk`；「V(-ctv)」= `-ctv`。标签上已直接标明对应参数，避免混淆。
- **布局**：CPU线程、闪存注意力、KV缓存K/V 四个控件现在排在**同一行**，一眼看全。
- `f16`：默认，精度与占用均衡。
- `q8_0` / `q4_0`：量化缓存，更省显存 → 同样显存能开更长上下文，精度略降（q4_0 更省但质量更低）。
- `f32`：精度最高，最占显存。
- **常见搭配**：K 用 `q8_0`、V 用 `f16`，兼顾精度与显存；两边都设 `q8_0` 则更省。
- **用途**：开大上下文（128K/256K/512K）显存不够时，优先把这里降到 `q8_0`。

### 6. 连续批处理（--cont-batching）
- **是什么**：是否启用连续批处理（continuous batching）。
- **原理**：传统批处理要等整批请求全部生成完才接收新请求；开启后，**正在解码的批次中随时可以插入/移除请求**——某个客户端回答结束立刻腾出算力给排队的新请求，不用空等。
- **效果**：多客户端并发时总吞吐量明显提升、等待时间变短；单客户端使用时无影响。
- 多客户端场景强烈建议保持开启（默认开）。
- **布局**：本项与 `mlock 锁定内存`、`禁用 mmap`、`启用 MTP` 及 MTP 状态提示**排在高级参数区同一行**，便于集中开关。

### 7. 温度（--temp）
- **是什么**：采样随机性/创造力的控制旋钮。
- 数值越低越保守、严谨、可预测；越高越发散、有创意。
- `0`：每次选概率最高的词 → 趋于稳定，但可能**复读**。
- 常用区间：**0.3 ~ 1.2**。默认 0.8。写代码建议 0.2~0.5，闲聊创意可 0.9~1.1。

### 8. Top-P（核采样）
- **是什么**：只从「累计概率达到该值」的最优候选词里选。
- `0.95` 默认；`1.0` 关闭；越小越保守。与温度配合使用。

### 9. Top-K
- **是什么**：只从「概率最高的前 K 个」候选词里选。
- `40` 默认；`0` 关闭；越小越保守。

### 10. 重复惩罚（--repeat-penalty）
- **是什么**：对重复出现的词施加惩罚，抑制复读。
- `1.0` = 关闭；**建议 1.0 ~ 1.3**。
- **用途**：模型「无限复读 / 同一句话循环」时，把它调到 1.1~1.3 能明显缓解 —— 这是对抗幻觉复读最重要的参数之一。

### 11. API Key
- **是什么**：调用 OpenAI 兼容接口所需的密钥。
- 设置后，客户端（opencode、Cherry Studio、Codex 等）必须携带该 Key 才能调用接口，否则被拒绝。
- 留空 = 不设密钥，任何人（能连到这个端口的人）都能调用。**局域网开放访问时务必设置。**

### 12. 超时（秒）（--timeout）
- **是什么**：服务对单个请求的读写超时时间。
- 请求超过该秒数会被强制断开，默认 3600（1 小时）。一般不用改。

### 13. mlock 锁定内存
- **是什么**（--mlock）：把模型数据锁定在内存中，防止被系统换出到磁盘。
- 代价：占更多物理内存；收益：推理更稳定、避免卡顿。内存充足时可开启。

### 14. 禁用 mmap
- **是什么**（--no-mmap）：关闭内存映射方式加载模型。
- 加载变慢，但个别磁盘/内存环境下能改善稳定性。默认关闭（即使用 mmap）。

### 15. 启动成功后自动打开浏览器
- 服务就绪后自动用默认浏览器打开 `http://监听地址:端口/` 的 Web 聊天界面。取消勾选则只启动服务、不弹浏览器。

### 16. 采样预设（生成参数组内下拉）
- **是什么**：一键应用一组采样参数（温度 / Top-P / Top-K / 重复惩罚），共四个选项：
  - **自定义**（默认）：不做联动，各参数自行调节；
  - **严谨（写代码/翻译）**：温度 `0.3`、Top-P `0.85`、Top-K `50`、重复惩罚 `1.1`，随机性低，适合代码、翻译等要求稳定准确的场景；
  - **均衡（日常对话）**：温度 `0.8`、Top-P `0.95`、Top-K `40`、重复惩罚 `1.0`，日常通用；
  - **创意（写作/脑洞）**：温度 `1.2`、Top-P `0.98`、Top-K `60`、重复惩罚 `1.0`，随机性高，适合创作、头脑风暴。
- 选中预设后四个数值框同步更新，状态栏提示「已应用采样预设: <预设名>」（3 秒）；**手动改动任一采样参数会自动回到「自定义」**。各参数的含义见上方 #7 ~ #10。

---

## 三、底部按钮

### 启动服务 / 停止服务
- **启动**：先检查端口占用 → 隐藏命令行窗口后台启动 `llama-server.exe` → 轮询 `/health` 就绪后（可选）自动打开浏览器。
- **停止**：用 `taskkill /T` 强制终止进程树。**模型跑飞、幻觉复读、停不下来时，一键点它**。

### 打开网页
- 在浏览器打开 llama.cpp Web 聊天界面。

### 打开 Agent 工具
- 打开「Agent 工具」列表中**当前高亮的那一行**对应的工具。程序会**记住上次选中的工具**，下次打开自动高亮它，方便快速再开。
- 服务运行时打开工具会自动连接当前 llama 服务（详见「四、Agent 工具」）。

### 保存配置
- 把**当前所有设置**写入本目录下的 `config.json`，下次启动自动恢复。包括：
  - 服务器全部参数（端口、GPU 层数、上下文、采样、API Key 等）
  - 所选主模型 / 视觉模型
  - Agent 工具手动指定的路径、上次选中的工具
  - 主题配色（深色/浅色/橙色/绿色/灰色/棕色）、窗口位置大小
- 程序在「启动服务」「退出程序」时也会自动保存。
- 也可通过菜单「工具 → 导出配置 / 导入配置」用 `.aic` 文件（内容即 JSON）备份与还原完整配置。

### 校验启动文件（第 2 排，6 号位）
- 选择任意 `.bat/.cmd/.ps1/.sh/文本脚本`，静态校验能否正常拉起 llama-server：可执行文件是否存在、模型/视觉模型是否就位、端口是否空闲等，并逐行生成参数报告。
- **bat 规范化能力**（本次增强）：自动收集 `set VAR=value` 变量并替换 `%VAR%` 引用（含 `%%` 转义、环境变量回退、`%~dp0`=脚本目录）；行尾 `^` 续行会合并成一条完整命令再解析（支持空格分行书写）；`REM`/`::` 注释行跳过，不会把注释里的 `-n cap` 之类误识别成参数。
- 左列可编辑、右列只读说明；「重新校验」按当前编辑内容重算，「保存配置」按原顺序写回源文件（bat 建议 GBK）。

### 导出启动文件（第 2 排，7 号位）
- 把**当前模型与全部参数**导出为可双击直接启动 `llama-server` 的 `.bat` 一键启动脚本，并在桌面创建带软件图标的快捷方式。等同菜单「工具 → 导出一键启动 (.bat)」。
- **不会**在导出目录产生额外的 `.ico` 图标文件：快捷方式直接使用启动器 exe 内嵌图标（源码运行时用 `assets/icon.ico`），仅当两者都缺失时才在系统临时目录生成一个回退图标。

---

## 四、Agent 工具

### 这是什么
内置几个常用 AI Agent / 客户端的快捷入口，自动在系统里检测它们的安装位置，一键打开；服务运行时打开还能自动连接当前 llama 服务。

### 内置工具（4 个）
| 工具 | 连接方式 |
|------|---------|
| **opencode** | 服务运行时自动生成 `opencode.json`（provider 指向当前服务），新终端打开 |
| **OpenCode 桌面版** | 服务运行时自动把连接信息复制到剪贴板 |
| **Claude Code** | 仅启动，不自动连接（llama.cpp 的 Anthropic 接口兼容性有限） |
| **llama-server** | 本启动器管理的本地服务 |

### 打开方式
- 终端类工具（opencode、Claude Code 等）：**新开一个终端窗口**运行（优先 Windows Terminal）。
- 应用类工具（OpenCode 桌面版 等）：直接启动程序。
- **双击某一行** = 打开；未找到的工具双击会提示手动指定可执行文件路径。

---

## 五、模型信息页（底部 Tab）

显示当前选中模型的 GGUF 头部信息（**离线读取，无需联网**）：
- 文件大小
- 架构（如 `qwen3next`、`gemma4`）
- 模型名称（模型自带的 `general.name`）
- **训练上下文**：模型**原生**支持的最大上下文（如 262144）。这是参考上限，启动器里设的上下文别超过它。
- 量化格式（如 Q4_K_S、Q6_K、BF16）
- 参数量（如 27B）

---

## 六、常见问题速查

| 现象 | 怎么调 |
|------|--------|
| 显存不够，模型加载失败 | GPU 层数改 `0` 或小数值；KV 缓存类型改 `q8_0`；上下文调小 |
| 想开更长上下文但显存不够 | KV 缓存改 `q8_0`/`q4_0`；闪存注意力 `on` |
| 模型复读、停不下来 | 重复惩罚调到 1.1~1.3 → 预测 Token 调小 → 温度适当调低 → 仍不行直接点「停止服务」 |
| 回答太死板/太跳 | 温度：死板→调低，太跳→调低（或调高 Top-P） |
| 多个客户端同时用 | 并行 Slots 设 2~4，保持连续批处理开启 |
| 局域网其他设备连不上 | 监听地址改 `0.0.0.0`，并务必设置 API Key |
| 端口被占用 | 换一个端口，或先停止占用端口的程序 |

---

## 七、新功能速览

### 1. 模型列表排序（基本参数区，位于「主模型」右侧）
- **位置**：紧挨「主模型」下拉框的右侧，保持原有排序功能；当前所选模型的完整路径显示在**底部状态栏左侧**。
- **目录+文件名**（默认）：先按所在目录、再按文件名从小到大（自然排序，`2 < 10`）。
- **大小降序**：大文件在前。**名称**：仅按文件名从小到大。

### 2. KV 缓存 K / V 独立设置（高级参数区）
- 「KV缓存K(-ctk)」与「V(-ctv)」两个下拉各自独立，可设不同量化类型，标签直接标明对应参数；与 CPU线程、闪存注意力排在同一行，详见「二、5」。

### 3. 多显卡（Multi-GPU 折叠面板）
- 点「检测 GPU」运行 `llama-server --list-devices` 列出本机设备（CUDA0/CUDA1…），勾选要用的卡：
  - **只勾一张** = 指定只用该卡；**勾 ≥2 张** = 多卡拆分，并启用以下参数：
    - **拆分模式**（`-sm`）：layer / row / tensor / none。
    - **tensor-split**（`-ts`）：各卡算力权重比例，如 `3,1`；留空 = 均分。
    - **主 GPU**（`-mg`）：中间结果/KV 归属卡索引，一般保持 0。
  - 不勾选任何卡 = llama.cpp 自动选卡（与旧版行为一致）。

### 4. MTP 多 token 预测（高级参数区「启用 MTP」复选框）
- **位置**：与 连续批处理、mlock、mmap 排在**同一行**；MTP 状态提示在同一行右侧，用**颜色 + 下划线**标识。
- 选择主模型后**后台自动检测**该 GGUF 是否含 MTP 能力（支持多文件分片模型），识别以下特征：
  - 张量名含 `mtp`、`nextn`、`next_n`、`eh_proj`、`shared_head`；
  - 或元数据声明 `nextn_predict_layers > 0`。
- 检测结果：
  - 「✔ 检测到 MTP（N 个特征），可启用」（绿色）→ **开放点选**；勾选后命令加 `--spec-type draft-mtp`，用模型自带 MTP 头做投机解码、提升生成速度。
  - 「未检测到 MTP 张量，如确认支持可手动勾选」（橙色）→ **不再强制禁用**（避免误判），复选框仍可手动勾选。
  - 「MTP 无法检测（文件不可读）」→ 同样保持可手动勾选。

### 5. 模型下载 Tab（HuggingFace）
- **左右双列**：左列 = 模型仓库；右列 = 选中仓库内的 `.gguf` 文件。
- **三栏检索（并/或）**：
  - **A 组织/作者**：默认 `unsloth`，可改其他组织（如 `Qwen`）；
  - **B 模型名称/版本**：如 `Qwen3-8B`；
  - **C 辅助关键词**：在返回结果中再做一次本地过滤（如 `GGUF`）；
  - **匹配**：并（同时满足）/ 或（任一满足）。
- **每页数量**：可选 20/50/100/200/500；「↓ 加载更多」按页大小累加（上限 1000）。
- **仓库表**：模型仓库 / 下载量 / 点赞 / 最近更新（= 仓库在 HuggingFace 上最近一次提交更新的日期）；**点击表头即可排序**，列宽可拖动、双击表头自适应。
- **文件表**：文件名 / 大小 / 已安装 / 下载 / 进度；**点击表头按文件名或大小排序**。每行有独立「下载」按钮与进度条，**支持断点续传**。
- **点击已安装文件**：自动把该文件设为上方「主模型」，并触发展开的后续逻辑（模型信息、视觉模型配对、MTP 检测、命令预览）。
- **下载目录**：「保存到」默认显示主页的**模型目录**，下载时在其下按**仓库名**建立子文件夹；可手动改为任意目录。
- **打开下载目录**：一键在资源管理器中打开（已选仓库时打开其子目录）。
- **底部区域**：左侧「仓库信息」（下载量/点赞/最近更新/流水线/标签），右侧「操作日志」；两列与底部之间有一根**可上下拖动的比例条**，可自由调整表格与信息区的高度比例。
- **检索实现**：只返回含 GGUF 的仓库（HF `filter=gguf`）；「最近更新」需 `full=true` 才由接口返回，已做兼容回退。

### 6. 导出命令预览为 .bat（菜单「工具 → 导出命令预览 (.bat)…」）
- 把「命令预览」页里的完整命令行**原样**保存为 `.bat`，可自选存储路径；双击即可启动服务。
- 另有「导出一键启动 (.bat)」：按当前模型与参数生成批处理并在桌面创建带图标的快捷方式。

### 7. 视觉模型同目录过滤（基本参数区）
- 选择主模型后，「视觉模型」下拉**只列出与该主模型同一目录下**的 mmproj，保证配套一致；无则显示「（不使用）」。

### 8. 更新 llama.cpp Tab（版本 / 分支源码）
- **Release 版本**：拉取 GitHub Release 列表（可选任意 tag），下载 Windows x64 预编译包后**安全安装**——只覆盖目标目录中的同名文件，旧文件自动备份为 `.bak`；模型、配置等其他文件一律不读不改不删。
- **分支源码**：选择/输入任意分支（如 `main`），断点续传下载该分支源码 zip，解压到「源码目录」下的 `llama.cpp-<分支>` 子目录（需自行编译）；同样只写该子目录内部，不影响其他路径的文件。

### 9. 模型统计（菜单「工具 → 模型统计…」独立界面）
- **按模型**：打开次数、使用时长、输入 Token、输出 Token。
- **时间明细**：按小时 / 日 / 月三种粒度汇总；时间范围可选 全部 / 今天 / 最近一周 / 本月（自然月）。
- Token 数来自 llama-server 运行日志（`prompt eval time` = 输入，`eval time` = 输出）；数据保存在数据目录 `stats.json`，可导出 CSV。

### 10. 启动文件校验增强（「校验启动文件」按钮）
- 支持 **bat 变量**：自动收集 `set VAR=value` / `set "VAR=value"` 并替换 `%VAR%`（含 `%%` 转义、环境变量回退、`%~dp0`=脚本目录）。
- 支持 **^ 续行合并**：跨多行的命令（每行以 `^` 结尾）会先拼成一条完整命令行再解析，空格分行书写也能识别。
- **REM/:: 注释跳过**：注释里的 `-n cap` 之类不再被误识别为参数；查找可执行文件时也会跳过 `set EXE=llama-server.exe` 这类变量定义行。
- 参数识别表扩充：新增 `--mmproj`(视觉模型，含文件就位检查)、`-fa`、`-dev`、`-ctk/-ctv`、`--temp/--top-k/--top-p/--repeat-penalty/--timeout`、`-a`(别名)、`-ub`、`-np`、`-sm/-mg`、推理格式，以及布尔开关 `--cont-batching/-cmoe/--context-shift/--mlock/--no-mmap`（不吞下一个参数）。

### 11. 导出启动文件按钮（第 2 排 7 号位）
- 原「预留功能」改为**导出启动文件**：一键把当前模型与全部参数导出为 `.bat` 并创建桌面快捷方式，无需再进菜单。

### 12. 数字框不再响应鼠标滚轮 + 布局调整
- 所有数值框（spinbox）**忽略滚轮事件**：滚动窗口/参数区经过数字框时不会再误改数值（之前表现为「数字只能往下调」）。上下箭头与直接键入不受影响（上下箭头已显式绘制，加/减都清晰可点）。
- **生成参数区**：三组「标签 + 控件」均匀铺满整行，标签不再被挤压隐藏。
- **高级参数区**：CPU线程 / 闪存注意力 / KV缓存K(-ctk) / KV缓存V(-ctv) 同一行；连续批处理 / mlock / mmap / MTP + MTP状态提示同一行；API Key / 超时 / 自动打开浏览器同一行。各控件**平均分布**，CPU线程与 API Key 不再独占宽度。
- **MTP** 检测不到时不再自动置灰，可手动勾选（详见「七、4」）。

### 13. 主题配色（底部状态栏右侧「🎨 主题」菜单）
- 内置 **6 套配色**：深色 / 浅色 / 橙色 / 绿色 / 灰色 / 棕色。点击状态栏右侧「🎨 主题」按钮弹出菜单选择，选择自动保存，下次启动恢复。
- 所有主题共用同一套尺寸度量（边框、内边距、字号），**切换主题不会改变窗口布局大小**。
- 浅色主题的分隔线/边框已加深，边界清晰。
- 配色方案集中定义在 `src/themes.py`，便于自行扩展。

### 14. 最小化到系统托盘（默认开启）
- 点窗口右上角「×」**不会退出**，而是缩进系统托盘并提示：「已最小化到托盘，点击图标恢复窗口。服务仍在后台运行。」
- 双击托盘图标恢复主窗口；右键菜单为：显示主窗口 / 启动服务 / 停止服务 / **退出**。只有「退出」才真正结束程序——若此时服务还在跑，会先询问「服务正在运行，退出时是否同时停止服务？」（是 = 停掉服务并退出；否 = 只关启动器、服务留在后台继续跑；取消 = 不退出）。

---

*由 LLama 启动器自动生成的本说明文档，供用户快速上手与排查。*
