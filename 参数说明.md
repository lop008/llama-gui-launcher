# LLama 启动器 · 参数说明

本文档对界面中每一个设置项做详细说明。界面中鼠标悬停在任意参数上也会显示简版提示。

---

## 一、基本参数（默认展开）

### 1. llama 目录
- **是什么**：llama.cpp 的安装目录。
- **要求**：目录下应有 `llama-server.exe` 及配套的 DLL（`llama.dll`、`ggml-*.dll` 等）。这是从官方 Release 解压后得到的完整文件夹。
- **作用**：程序从这里读取 `llama-server.exe` 来启动服务，并在此目录（含所有子文件夹）内递归扫描模型。

### 2. 主模型
- **是什么**：要加载的 GGUF 格式模型文件（`*.gguf`）。
- **怎么选**：程序自动扫描 llama 目录下所有 `.gguf` 文件（自动跳过未下载完成的 `.xltd` / `.tmp` 等文件），下拉列表中显示文件名与大小。你只需要点选。
- **视觉模型提示**：若该模型是「多模态/视觉模型」，通常需要配合一个 `mmproj` 文件一起加载。

### 3. 视觉模型（mmproj）
- **是什么**：多模态视觉模型文件（`mmproj-*.gguf`），负责把图像编码成模型能理解的向量。没有它，模型只能处理纯文本。
- **自动配对规则**：**严格按目录配对** —— 只自动选中「与主模型在同一个目录下」的 mmproj，从而保证主模型与视觉模型是配套一致的。
  - 例如 `models/` 目录下同时有 `Qwythos-...BF16.gguf` 和 `mmproj-Qwythos-...F16.gguf`，选择前者时会自动选中后者。
  - 若主模型所在目录没有 mmproj（例如纯文本/代码模型），则默认「（不使用）」，不会错误地配到别的目录。
- **维度一致性校验**：启动前会自动读取主模型与视觉模型的嵌入维度（projection_dim / embedding_length）并比对。不一致时会**红色警告并弹出确认框**（如：主模型 2560 vs 视觉模型 5376），此时应更换配套的视觉模型，或选择「（不使用）」以纯文本模式运行。
- **提示**：下拉列表只显示文件名（悬停可看完整路径）；若有同名文件才附带所在文件夹名。
- 你也可以**手动**在下拉列表里改选其他目录的 mmproj，或选择「（不使用）」以纯文本模式运行。

### 4. 预设
- **是什么**：根据显卡显存大小，一键设置「推荐的上下文长度」。
- **只改一个参数**：选择预设后**只修改「上下文」**这一项，其余参数一律不动。
- **对照表**：

| 预设 | 上下文长度 | 说明 |
|------|-----------|------|
| 4GB 显存 | 16K（16384） | 小显存也能跑 |
| 6GB 显存 | 32K（32768） | 默认推荐档 |
| 8GB 显存 | 64K（65536） | |
| 12GB 显存 | 128K（131072） | |
| 16GB 显存 | 256K（262144） | |
| 24GB 显存 | 512K（524288） | 长上下文、大显存 |

- **提示**：预设给的是「显存够用」的保守值；显存越少能开的上下文越短。想精确控制就选「自定义」，直接改「上下文」数值。

### 5. GPU 层数（-ngl）
- **是什么**：把模型计算层卸载到显卡显存的层数。
- **常用取值**：
  - `all`：尽可能全部放进显存，速度最快（显存放得下时首选）。
  - `auto`：由程序自动判断。
  - `0`：完全用 CPU 跑（不占显存，但慢很多）。
  - 具体数字（如 `10`、`40`）：手动指定只放前 N 层。
- **经验**：显存够大就 `all`；显存不够会加载失败，此时改为 `0` 或较小的数值，或用 CPU 模式。

### 6. 监听地址（Host）
- **是什么**：服务监听的网络地址。
- `127.0.0.1`：仅本机可访问（最安全，默认）。
- `0.0.0.0`：局域网内其他设备也能访问（注意安全，建议配合 API Key）。

### 7. 端口
- **是什么**：服务监听的端口，默认 `8080`。
- 启动成功后，OpenAI 兼容接口地址为 `http://监听地址:端口/v1`，Web 界面为 `http://监听地址:端口/`。
- 如果端口被占用，启动前程序会提示。

### 8. 上下文（-c，ctx size）
- **是什么**：上下文长度（单位：token），即模型**一次会话最多能「记住」多少内容**（你的历史对话 + 它的回答）。
- **数值含义**：值越大，模型能参考的对话越长，但占用的显存/内存越大、速度越慢。
- **上限参考**：「模型信息」页会显示该模型的「训练上下文」，例如 `262144`，意思是这个模型原生最多支持 262K token 的上下文。启动器里设的上下文应尽量**不超过**这个值（超过会因 KV 缓存不足而报错或异常）。
- **最小建议**：一般至少 4096，做代码/长文推荐 8192 以上；要跑 1M 上下文的模型（如你的 Qwythos-1M）则按预设选 512K。

### 9. 预测 Token（-n，n_predict）
- **是什么**：模型**每次回答最多生成的 token 数**。
- `-1` = 无限：一直生成直到模型输出结束符为止。
- 具体数字（如 8192）：限制单次回答最长 8192 token。
- **用途**：当模型「停不下来 / 无限复读 / 输出失控」时，调小这个值可以强行截断回答长度，是缓解幻觉复读的实用手段之一。

---

## 二、高级参数（默认折叠，按需打开）

### 1. CPU 线程（-t）
- **是什么**：用于计算（推理 + 部分 prompt 处理）的 CPU 线程数。
- `-1` = 自动（通常使用全部逻辑核心）。一般无需修改。

### 2. 批大小（-b，batch size）
- **是什么**：逻辑批大小，影响 prompt 处理速度。
- 默认 2048 通常就很好；除非排查性能问题，一般不用改。

### 3. 并行 Slots（-np，parallel）
- **是什么**：服务能**同时处理**多少个并发请求。
- `-1` = 自动。多客户端（opencode、Cherry Studio、浏览器同时用）建议设为 2~4。
- **代价**：每个 slot 会额外占用一份上下文内存，slot 越多越占显存。

### 4. 闪存注意力（-fa，Flash Attention）
- **是什么**：是否启用 Flash Attention 优化。
- `on`：推荐。显著省显存、提升长上下文速度（现代显卡均支持）。
- `off`：个别老显卡或兼容问题才需要关。
- `auto`：由程序判断。

### 5. KV 缓存类型（-ctk / -ctv）
- **是什么**：KV 缓存的存储精度，直接决定「上下文能占多大显存」。
- `f16`：默认，精度与占用均衡。
- `q8_0` / `q4_0`：量化缓存，更省显存 → 同样显存能开更长上下文，精度略降（q4_0 更省但质量更低）。
- `f32`：精度最高，最占显存。
- **用途**：开大上下文（128K/256K/512K）显存不够时，优先把这里降到 `q8_0`。

### 6. 连续批处理（--cont-batching）
- **是什么**：是否启用连续批处理。
- 开启后多个并发请求能更高效地共享算力，多客户端场景强烈建议保持开启（默认开）。

### 7. 温度（--temp）
- **是什么**：采样随机性/创造力的控制旋钮。
- 数值越低越保守、严谨、可预测；越高越发散、有创意。
- `0`：每次选概率最高的词 → 趋于稳定，但可能**复读**。
- 常用区间：**0.3 ~ 1.2**。默认 0.8。写代码建议 0.2~0.5，闲聊创意可 0.9~1.1。

### 8. Top-P（核采样）
- **是什么**：只从「累计概率达到该值」的最优候选词里选。
- `0.95` 默认；`1.0` 关闭；越小越保守。与温度配合使用。

### 9. Top-K
- **是什么**：只从「概率最高的前 K 个」候选词里选。
- `40` 默认；`0` 关闭；越小越保守。

### 10. 重复惩罚（--repeat-penalty）
- **是什么**：对重复出现的词施加惩罚，抑制复读。
- `1.0` = 关闭；**建议 1.0 ~ 1.3**。
- **用途**：模型「无限复读 / 同一句话循环」时，把它调到 1.1~1.3 能明显缓解 —— 这是对抗幻觉复读最重要的参数之一。

### 11. API Key
- **是什么**：调用 OpenAI 兼容接口所需的密钥。
- 设置后，客户端（opencode、Cherry Studio、Codex 等）必须携带该 Key 才能调用接口，否则被拒绝。
- 留空 = 不设密钥，任何人（能连到这个端口的人）都能调用。**局域网开放访问时务必设置。**

### 12. 超时（秒）（--timeout）
- **是什么**：服务对单个请求的读写超时时间。
- 请求超过该秒数会被强制断开，默认 3600（1 小时）。一般不用改。

### 13. mlock 锁定内存
- **是什么**（--mlock）：把模型数据锁定在内存中，防止被系统换出到磁盘。
- 代价：占更多物理内存；收益：推理更稳定、避免卡顿。内存充足时可开启。

### 14. 禁用 mmap
- **是什么**（--no-mmap）：关闭内存映射方式加载模型。
- 加载变慢，但个别磁盘/内存环境下能改善稳定性。默认关闭（即使用 mmap）。

### 15. 启动成功后自动打开浏览器
- 服务就绪后自动用默认浏览器打开 `http://监听地址:端口/` 的 Web 聊天界面。取消勾选则只启动服务、不弹浏览器。

---

## 三、底部按钮

### 启动服务 / 停止服务
- **启动**：先检查端口占用 → 隐藏命令行窗口后台启动 `llama-server.exe` → 轮询 `/health` 就绪后（可选）自动打开浏览器。
- **停止**：用 `taskkill /T` 强制终止进程树。**模型跑飞、幻觉复读、停不下来时，一键点它**。

### 打开网页
- 在浏览器打开 llama.cpp Web 聊天界面。

### 打开 Agent 工具
- 打开「Agent 工具」列表中**当前高亮的那一行**对应的工具。程序会**记住上次选中的工具**，下次打开自动高亮它，方便快速再开。
- 服务运行时打开工具会自动连接当前 llama 服务（详见「四、Agent 工具」）。

### 保存配置
- 把**当前所有设置**写入本目录下的 `config.json`，下次启动自动恢复。包括：
  - 服务器全部参数（端口、GPU 层数、上下文、采样、API Key 等）
  - 所选主模型 / 视觉模型
  - Agent 工具手动指定的路径、上次选中的工具
  - 主题（深色/浅色）、窗口位置大小
- 程序在「启动服务」「退出程序」时也会自动保存。
- 也可通过菜单「工具 → 导出配置 / 导入配置」用 `.aic` 文件（内容即 JSON）备份与还原完整配置。

---

## 四、Agent 工具

### 这是什么
内置几个常用 AI Agent / 客户端的快捷入口，自动在系统里检测它们的安装位置，一键打开；服务运行时打开还能自动连接当前 llama 服务。

### 内置工具（4 个）
| 工具 | 连接方式 |
|------|---------|
| **opencode** | 服务运行时自动生成 `opencode.json`（provider 指向当前服务），新终端打开 |
| **OpenCode 桌面版** | 服务运行时自动把连接信息复制到剪贴板 |
| **Claude Code** | 仅启动，不自动连接（llama.cpp 的 Anthropic 接口兼容性有限） |
| **llama-server** | 本启动器管理的本地服务 |

### 打开方式
- 终端类工具（opencode、Claude Code 等）：**新开一个终端窗口**运行（优先 Windows Terminal）。
- 应用类工具（OpenCode 桌面版 等）：直接启动程序。
- **双击某一行** = 打开；未找到的工具双击会提示手动指定可执行文件路径。

---

## 五、模型信息页（底部 Tab）

显示当前选中模型的 GGUF 头部信息（**离线读取，无需联网**）：
- 文件大小
- 架构（如 `qwen3next`、`gemma4`）
- 模型名称（模型自带的 `general.name`）
- **训练上下文**：模型**原生**支持的最大上下文（如 262144）。这是参考上限，启动器里设的上下文别超过它。
- 量化格式（如 Q4_K_S、Q6_K、BF16）
- 参数量（如 27B）

---

## 六、常见问题速查

| 现象 | 怎么调 |
|------|--------|
| 显存不够，模型加载失败 | GPU 层数改 `0` 或小数值；KV 缓存类型改 `q8_0`；上下文调小 |
| 想开更长上下文但显存不够 | KV 缓存改 `q8_0`/`q4_0`；闪存注意力 `on` |
| 模型复读、停不下来 | 重复惩罚调到 1.1~1.3 → 预测 Token 调小 → 温度适当调低 → 仍不行直接点「停止服务」 |
| 回答太死板/太跳 | 温度：死板→调低，太跳→调低（或调高 Top-P） |
| 多个客户端同时用 | 并行 Slots 设 2~4，保持连续批处理开启 |
| 局域网其他设备连不上 | 监听地址改 `0.0.0.0`，并务必设置 API Key |
| 端口被占用 | 换一个端口，或先停止占用端口的程序 |

---

*由 LLama 启动器自动生成的本说明文档，供用户快速上手与排查。*
